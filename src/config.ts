const LOCAL_IP = "192.168.1.200"; // Buraya kendi IP adresini yaz

const CONFIG = {
  API_URL: `http://${LOCAL_IP}:5000/api/word`, // IP'yi otomatik olarak API URL'ye ekliyor
};

export default CONFIG;
